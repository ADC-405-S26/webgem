% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/metricRank.R
\name{metricRank}
\alias{metricRank}
\title{Rank numeric values using one or two thresholds}
\usage{
metricRank(
  x,
  threshold,
  upper_threshold = NULL,
  lower_label,
  middle_label = NULL,
  upper_label
)
}
\arguments{
\item{x}{A numeric vector to label}

\item{threshold}{The cutoff value used to separate lower values from higher values.
Values below this number receive \code{lower_label}, values above receive \code{upper_label}.}

\item{upper_threshold}{An optional upper cutoff value. If provided, values between \code{threshold} and \code{upper_threshold} receive the middle label.}

\item{lower_label}{The label for values below \code{threshold}.}

\item{middle_label}{The label for values between \code{threshold} and \code{upper_threshold}. Only used when \code{upper_threshold} is provided.}

\item{upper_label}{The label for values at or above the upper cutoff/}
}
\value{
A character vector or labels
}
\description{
This function labels numeric values based on user-defined threshold values.
With one threshold, values are labeled as lower or upper. With two thresholds,
values are labeled as lower, middle, or upper. This function is useful for
creating categories from numeric metrics, especially when preparing data for
summaries or visualization.
}
\examples{
era = c(2.03, 2.40, 5.49, 4.23, 3.46, 1.01, 4.50, 3.98, 3.76)

metricRank(
  x = era,
  threshold = 2.75,
  upper_threshold = 3.75,
  lower_label = "great era",
  middle_label = "mediocre era",
  upper_label = "bad era")
}
